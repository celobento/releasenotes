<p id="notice"><%= notice %></p>

  


            <div class="panel panel-default panel-table">
              <div class="panel-heading">
                <div class="row">
                  <div class="col col-xs-6">
                    <h3 class="panel-title">Releases Note</h3>
                  </div>
                  <div class="col col-xs-6 text-right">
                    
                    <%= link_to new_release_note_path(tab:"descricao"), class:"btn btn-primary btn-sm active" do %>
                      <span class="glyphicon glyphicon-plus"></span> Novo
                    <% end %>
                  </div>
                </div>
              </div>
              <div class="panel-body table-responsive">
                <table class="table table-striped table-bordered table-list">
                  <thead>
                    <tr>
                        <tr>
                          <th><span class="glyphicon glyphicon-cog"></span></th>
                          <th>Sistema</th>
                          <th>Contexto</th>
                        </tr>
                    </tr> 
                  </thead>
                  <tbody>
                    <% @release_notes.each do |release_note| %>
                      <tr>
                        <td align="center">
                          
                              <div class="form-inline">
                               <%= link_to release_note_path(release_note, tab:"descricao"), class:"btn btn-default btn-sm" do %>
                                  <span class="glyphicon glyphicon-pencil"></span>
                                <% end %>
                              </div>
                            
                        </td>
                        <td><%= release_note.sistema.nome %></td>
                        <td><%= release_note.contexto %></td>
                      </tr>
                    <% end %>
                          
                        </tbody>
                </table>
            
              </div>
              <div class="panel-footer">
                <div class="row">
                  <div class="col col-xs-4">
                    <%= page_entries_info @release_notes %>
                  </div>
                  <div class="col col-xs-8">
                    <ul class="pagination hidden-xs pull-right">
                      <li><%= will_paginate @release_notes, :previous_label => image_tag('previous.png', :alt => 'Anterior', :class => 'glyphicon glyphicon-chevron-left') ,:next_label => image_tag('next.png', :alt => 'Próximo', :class => 'btn_img') , :separator => ' | ', :params => params %> </li>
                    </ul>
                  </div>
                </div>
              </div>
            </div>

